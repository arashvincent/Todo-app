import React from 'react'

const Hello = (props) => {

    const lists = [1, 2, 4, 6]


    return (
        <div>
            {/* <button onClick={() => { props.HelloWorld("arash") }}>clickk</button>
            {lists.map((list) => { return <li>{list * 2}</li> })} */}
        </div>
    )
}

export default Hello
